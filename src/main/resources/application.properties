# =========================================
# 🔌 CONEXIÓN ORACLE CLOUD (Autonomous DB)
# =========================================

# Usa EXACTAMENTE el alias que viste en tnsnames.ora
# Ejemplo (cambia si tu alias es distinto):
spring.datasource.url=jdbc:oracle:thin:@qdi2qhy86mi2lln7_high?TNS_ADMIN=src/main/resources/Wallet_QDI2QHY86MI2LLN7

# 👇 IMPORTANTE:
# - username: el usuario de la base de datos (normalmente ADMIN o el usuario que te dieron)
# - password: la contraseña de ESA base de datos, NO la del wallet ZIP, ni la de tu cuenta Oracle Cloud.
spring.datasource.username=ADMIN           # <-- cámbialo si tu profe te dio otro usuario
spring.datasource.password=Huerto-Hogar25

spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# =========================================
# ⚙️ JPA / HIBERNATE
# =========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# =========================================
# 🔐 SEGURIDAD (JWT)
# (lo que ya tengas configurado, si hay más propiedades JWT, déjalas debajo)
spring.main.allow-bean-definition-overriding=true

# =========================================
# 🌐 SERVIDOR
# =========================================
server.port=8080
